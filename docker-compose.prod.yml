version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=https://digifortlabs.com/api
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production


