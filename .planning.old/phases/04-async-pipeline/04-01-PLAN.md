# Phase 4 Plan 1: Async Compression Pipeline

Enhance the asynchronous processing pipeline with granular status tracking, result retrieval, and automated maintenance.

## Proposed Changes

### [Backend] API Enhancements

#### [MODIFY] [optimization.py](file:///d:/Website/DIGIFORTLABS/backend/app/routers/optimization.py)
- Add `GET /download/{job_id}/{filename}` endpoint to retrieve optimized PDFs.
- Update `get_optimization_status` to support more detailed task metadata (metrics, progress).

### [Backend] Task Logic

#### [MODIFY] [optimization.py](file:///d:/Website/DIGIFORTLABS/backend/app/tasks/optimization.py)
- Use `self.update_state()` to report progress steps (e.g. "Initializing", "Compressing", "Finalizing").
- Improve error serialization for the task result.

### [Backend] Maintenance

#### [MODIFY] [cleanup_service.py](file:///d:/Website/DIGIFORTLABS/backend/app/services/cleanup_service.py)
- Add a method to clean up specific job IDs.
- Ensure the 24-hour loop is robust and logs results.

## Verification Plan

### Automated Tests
- Integration test: Trigger a task, poll status until complete, and verify the download link works.
- Unit test: Verify `CleanupService` removes directories older than the threshold.

### Manual Verification
- Perform a full "Upload -> Wait -> Download" flow in the dashboard.
- Check backend logs for state transition messages.
